; Surcharge PHP pour hébergement mutualisé (LWS) où php.ini n'est pas modifiable.
; max_input_vars par défaut (1000) tronque silencieusement les formulaires volumineux
; (ex: édition du plan comptable avec plus de 1000 champs soumis en une fois).
max_input_vars = 20000
